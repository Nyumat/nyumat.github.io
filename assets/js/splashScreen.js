/*
Will contain launch screen javaScript code.
The main reason for this is because my website in its current state
Doesn't really have  a great landing/startup animation that is done yet.
*/

var onExit;
var user_cookie = document.cookie
$(selector).delay(2000).fadeIn("Animation");

      if visit == first_visit {
      
        onExit = $selector.timeDelay().click(function);
        user.cookie = "username=" + (name); expires=(onExit);
        
        $('.enter_link').click(function() {
               $(this).parent().fadeOut(500);
               
         };
      };
   };         
